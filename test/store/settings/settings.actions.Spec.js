describe('settings.actions', () => {
    it('setLoading', () => {
        throw new Error();
    });

    describe('thunk actions', () => {
        describe('loadSettings', () => {
            it('loads settings', () => {
                throw new Error();
            });

            it('has error', () => {
                throw new Error();
            });
        });

        describe('saveSettings', () => {
            it('saves settings', () => {
                throw new Error();
            });

            it('has error', () => {
                throw new Error();
            });
        });
    });
});
